import React from 'react'

const Title = ({title, subtitle}) => (
    <div>
        <p>
            {title}
        </p>

        <p>
            {subtitle}
        </p>

    </div>
  )

  export default Title
